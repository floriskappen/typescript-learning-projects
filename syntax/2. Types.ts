// types.ts

// Typescript can suggest functions
const today = new Date();
today.getMonth();

// Typescript knows that person has .age
const person = {
    age: 20
};
person.age

// Typescript knows that red is of type Color
class Color {

}
const red = new Color();